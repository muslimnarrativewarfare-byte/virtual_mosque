# Components Directory

This folder contains reusable UI and view components.

## Ownership boundaries
- `ui/`: design-system and primitive components.
- `layout/`: shells, headers, navigation, and page framing.
- `mosques/`: feature-specific components for mosque-related experiences.
- Keep components presentational where possible; move data fetching and domain logic to `lib/`.
