# Lib Directory

Shared application logic, service clients, and pure utility helpers live here.

## Ownership boundaries
- Keep framework-agnostic logic in `lib/` where possible.
- Avoid placing visual components in this folder.
- Add focused modules grouped by domain (for example, `lib/mosques/*`).
